class Solution {
public:
    int missingMultiple(vector<int>& nums, int k) {
      set<int>vec(nums.begin(),nums.end());
      int x=k;
      while(vec.count(x)){
        x+=k;
      }  
      return x;
    }
};
