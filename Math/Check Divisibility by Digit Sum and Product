class Solution {
public:
    bool checkDivisibility(int n) {
        int original = n;
        int sum = 0;
        int prd = 1;
        while (n > 0) {
            sum += n % 10;
            prd *= n % 10;
            n /= 10;
        }
        return (original % (sum + prd) == 0);
    }
};
